# Shared room relay (required for cross-device join).
# Local: npm run relay  →  http://127.0.0.1:45322
# Production: set to your deployed relay origin (https://…).
VITE_RELAY_URL=http://127.0.0.1:45322
